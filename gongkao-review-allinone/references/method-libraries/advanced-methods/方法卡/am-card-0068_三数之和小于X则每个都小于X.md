# 三数之和<X则每个都<X（正数推理）

- **方法名**: 三数之和<X则每个都<X（正数推理）
- **适用题型**: 资料分析 - 隐藏数据的逻辑推理判断
- **触发场景**: 表格只列部分数据，其余数据不可见但可算出总和，需要判断每个隐藏值是否满足某条件
- **步骤逻辑**:
  1. 若 N 个正数之和 < X，则可推出每个数都 < X
  2. 若 N 个正数之和 > X，则无法确定每个数都 > X（可能一大两小）
  3. 应用：算出隐藏省市的总量，若总量<阈值，则每个省市都<阈值，无需逐个知道
- **高频错因**: 反向推理错误——看到和大于X就以为每个都大于X；忘记数据必须是正数的前提
- **来源文件**: source-0014.md
- **confidence**: high
